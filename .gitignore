node_modules/
# dist/ tracked for Timeweb App Platform (Caddy serves project root / dist)
.astro/
.env
.env.*
!.env.example
*.log
.DS_Store
.idea/
.vscode/

# Сборочные копии вне dist/ (деплой — только `npm run build` → dist/)
# Сырые скриншоты; боевые кадры лежат в public/screens/
Screens/
# Генераторы инвойсов, сырая съёмка, seed-скрипты — не в прод
demo-assets/
